meta {
  name: Register User
  type: http
  seq: 1
}

post {
  url: {{HOST_API}}/api/v1/auth/signup
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{DEFAULT_USER_EMAIL}}",
    "password": "{{DEFAULT_USER_PWD}}",
    "fullName": "{{DEFAULT_USER_FULLNAME}}"
  }
}

settings {
  encodeUrl: true
}
